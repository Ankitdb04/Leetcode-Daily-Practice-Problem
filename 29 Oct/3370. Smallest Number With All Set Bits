class Solution {
    public int smallestNumber(int n) {
        int ans = 1;
        while (ans < n) {
            ans = (ans << 1) | 1;  // shift left and set all lower bits to 1
        }
        return ans;
    }
}
